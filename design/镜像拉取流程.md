# 镜像拉取流程

```shell
allrun image pull 镜像名:镜像版本
# 示例
allrun image pull registry.cn-shanghai.aliyuncs.com/tangxusc/alpine:3.18.0
```

默认将镜像拉取到宿主机`--images-store-dir`flag指定的目录下。

可选参数: 
- --tls-verify: 是否验证证书
- --images-store-dir: 镜像存储目录(在global flags中有此flag)